<Window x:Class="INTERMODULAR.MVVM.View.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:INTERMODULAR.MVVM.View"
        xmlns:viewModel="clr-namespace:INTERMODULAR.MVVM.ViewModel" xmlns:customcontrols="clr-namespace:INTERMODULAR.CustomControls"
        mc:Ignorable="d"
        Title="LoginView" Height="450" Width="800"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="Transparent"
        AllowsTransparency="True">

    <Window.DataContext>
        <viewModel:LoginViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibility"/>
    </Window.Resources>

    <Window.Visibility>
        <Binding Path="IsViewVisible" Mode="TwoWay" Converter="{StaticResource BooleanToVisibility}"/>
    </Window.Visibility>

    <Border Background="#FFDEDEDE" CornerRadius="20">
        <Grid>
            <Label Content="Usuario" HorizontalAlignment="Left" Margin="105,115,0,0" VerticalAlignment="Top"/>
            <Label Content="Contraseña" HorizontalAlignment="Left" Margin="105,181,0,0" VerticalAlignment="Top"/>
            <TextBox x:Name="tb_User" 
                     HorizontalAlignment="Left" 
                     Margin="105,141,0,0" 
                     TextWrapping="Wrap" 
                     Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" 
                     VerticalAlignment="Top" 
                     Width="120"/>
            <customcontrols:BindablePasswordBox Password="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="105,207,575,225"/>

            <TextBlock 
                HorizontalAlignment="Left" 
                Margin="106,238,0,0" 
                TextWrapping="Wrap" 
                Text="TextBlock" 
                VerticalAlignment="Top"
                Foreground="#D7596D" Width="119"/>

            <Button Command="{Binding LoginCommand}"
                    Content="LOGIN" 
                    HorizontalAlignment="Left" 
                    Margin="105,309,0,0" 
                    VerticalAlignment="Top" 
                    Height="25" 
                    Width="60"/>


        </Grid>
    </Border>
</Window>
